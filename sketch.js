var helicopterIMG, helicopterSprite, packageSprite,packageIMG;
var packageBody,ground 
const Engine = Matter.Engine;
const World = Matter.World;
const Bodies = Matter.Bodies;
const Body = Matter.Body;

function preload()
{
	
}

function setup() {
	createCanvas(800, 700);


	engine = Engine.create();
	world = engine.world;

	//packageBody = Bodies.circle(width/2 , 200 , 5 , {restitution:1.5, isStatic:false});
	//World.add(world, packageBody);
	

	//Create a Ground
	//ground = Bodies.rectangle(width/2, 650, width, 10 , {isStatic:true} );
 	//World.add(world, ground);


	Engine.run(engine);
  
}


function draw() {
  rectMode(CENTER);
  background(0); 

  roof1 = new roof (200,50,180,50);

  bob1 = new bob (50,300,30);
  bob2 = new bob (100,300,30);
  bob3 = new bob (150,300,30);
  bob4 = new bob (200,300,30);
  bob5 = new bob (250,300,30); 

  rope1 = new chain (bob1.body,roof1.body,-bob1Diameter*2,0);
  rope2 = new chain (bob2.body,roof1.body,-bob2Diameter*2,0);
  rope3 = new chain (bob3.body,roof1.body,-bob3Diameter*2,0);
  rope4 = new chain (bob4.body,roof1.body,-bob4Diameter*2,0);
  rope5 = new chain (bob5.body,roof1.body,-bob5Diameter*2,0);
  
  bob1.display();
  bob2.display();
  bob3.display();
  bob4.display();
  bob5.display();

  rope1.display();
  rope2.display();
  rope3.display();
  rope4.display();
  rope5.display();

  drawSprites();
 
}

































































































































































































































































































































































































































































































































































































